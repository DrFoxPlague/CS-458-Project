import GameModel from "../../schemas/Game";
import { GraphQLError } from "graphql";

export default {
    Query: {

    },
    Mutation: {
        createGame: async () => {},
        updateGame: async () => {},
        deleteGame: async () => {}
    }
}